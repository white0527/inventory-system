# === 伺服器設定 ===
server.port=8080

# === 資料庫連線設定 (Supabase Pooler IPv4) ===
# 根據您的截圖，正確主機是 aws-1，Port 是 6543
spring.datasource.url=jdbc:postgresql://aws-1-ap-southeast-1.pooler.supabase.com:6543/postgres?sslmode=require

# 帳號 (來自您的截圖)
spring.datasource.username=postgres.txowvwqkruzamxaxzosc

# 密碼 (維持原本)
spring.datasource.password=Eason052334090

# 驅動程式
spring.datasource.driver-class-name=org.postgresql.Driver

# === JPA 設定 ===
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# NAS 檔案路徑設定
app.nas.file.path=Z:/inventory.xlsx
app.nas.report.path=Z:/reports/