# === 伺服器設定 ===
server.port=8080

# === 資料庫連線設定 (依照截圖 image_aa9504.png 使用 Session Pooler) ===
# 使用 5432 埠號配合 Session 模式，這最適合 Hibernate 的自動建表功能
spring.datasource.url=jdbc:postgresql://aws-1-ap-southeast-1.pooler.supabase.com:5432/postgres?sslmode=require&tcpKeepAlive=true&reWriteBatchedInserts=true
spring.datasource.username=postgres.txowvwqkruzamxaxzosc
spring.datasource.password=Eason052334090
spring.datasource.driver-class-name=org.postgresql.Driver

# === 🛡️ 連線池極限優化 (避免擠爆 Supabase 免費版連線數) ===
# 限制最大連線數為 2，這能確保您的專案不會超過 15 個連線的限制
spring.datasource.hikari.maximum-pool-size=2
spring.datasource.hikari.minimum-idle=1
# 增加等待時間到 30 秒，給遠端網路更多連線緩衝，避免 SocketTimeout
spring.datasource.hikari.connection-timeout=30000
# 縮短連線壽命，強制釋放「幽靈連線」資源
spring.datasource.hikari.max-lifetime=600000
spring.datasource.hikari.idle-timeout=30000

# === JPA 設定 (自動同步資料庫欄位) ===
# update 模式會自動幫您在資料庫建立「成本價」等欄位
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
# 讓 Hibernate 自動選擇最適合 PostgreSQL 的語法
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect