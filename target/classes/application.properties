# ==========================================================
# 祥易查價系統 - 核心設定檔 (2026-02-15 穩定版)
# ==========================================================

# === 伺服器設定 ===
server.port=8080

# === 資料庫連線設定 (優化跨國連線超時問題) ===
# 1. 改用 6543 埠號配合 Transaction Pooler 模式，解決 SocketTimeout
# 2. 增加 connectTimeout=30 與 socketTimeout=30 緩衝網路延遲
spring.datasource.url=jdbc:postgresql://aws-1-ap-southeast-1.pooler.supabase.com:6543/postgres?sslmode=require&connectTimeout=30&socketTimeout=30&tcpKeepAlive=true
spring.datasource.username=postgres.txowvwqkruzamxaxzosc
spring.datasource.password=Eason052334090
spring.datasource.driver-class-name=org.postgresql.Driver

# === 🛡️ 連線池優化 (防止擠爆 Supabase 限制) ===
# 限制最大連線數為 4，並增加連線建立的等待時限
spring.datasource.hikari.maximum-pool-size=4
spring.datasource.hikari.minimum-idle=2
spring.datasource.hikari.connection-timeout=30000
spring.datasource.hikari.validation-timeout=5000
spring.datasource.hikari.max-lifetime=600000

# === JPA / Hibernate 設定 (加速啟動關鍵) ===
# 修正重點：改為 none 停止啟動時的欄位比對，解決啟動崩潰
spring.jpa.hibernate.ddl-auto=none
spring.jpa.show-sql=true
# 顯式指定 Dialect，避免 Hibernate 猜錯版本導致報錯
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# === 批次處理優化 (針對 Excel 匯入與同步) ===
spring.jpa.properties.hibernate.jdbc.batch_size=50
spring.jpa.properties.hibernate.order_inserts=true
spring.jpa.properties.hibernate.order_updates=true

# === 記錄檔設定 ===
logging.level.org.hibernate.SQL=DEBUG
logging.level.org.hibernate.type.descriptor.sql.BasicBinder=TRACE