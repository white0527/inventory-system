# === 1. 伺服器設定 ===
server.port=8080

# === 2. 🛡️ Hikari 連線池優先設定 (防止啟動封印) ===
spring.datasource.hikari.initialization-fail-timeout=-1

# === 3. 資料庫連線字串 (加入關鍵 prepareThreshold 參數) ===
# 這裡將 connectTimeout 稍微拉長，並確保 prepareThreshold=0 解決 Pooler 衝突
spring.datasource.url=jdbc:postgresql://aws-1-ap-southeast-1.pooler.supabase.com:6543/postgres?sslmode=require&prepareThreshold=0&connectTimeout=60&socketTimeout=60&tcpKeepAlive=true
spring.datasource.username=postgres.txowvwqkruzamxaxzosc
spring.datasource.password=Eason052334090
spring.datasource.driver-class-name=org.postgresql.Driver

# === 4. Hikari 運行優化 ===
# 拉高至 60 秒以對抗跨國網路延遲
spring.datasource.hikari.connection-timeout=60000
spring.datasource.hikari.maximum-pool-size=4
spring.datasource.hikari.minimum-idle=2
spring.datasource.hikari.validation-timeout=5000
spring.datasource.hikari.max-lifetime=600000

# === 5. JPA / Hibernate 設定 ===
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.open-in-view=false
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# === 6. 批次處理優化 ===
spring.jpa.properties.hibernate.jdbc.batch_size=50
spring.jpa.properties.hibernate.order_inserts=true
spring.jpa.properties.hibernate.order_updates=true